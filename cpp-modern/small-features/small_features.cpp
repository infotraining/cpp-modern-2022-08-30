#include <algorithm>
#include <iostream>
#include <list>
#include <map>
#include <numeric>
#include <string>
#include <tuple>
#include <vector>
#include <span>

#include "catch.hpp"

enum class DayOfWeek : uint8_t { mon = 1, tue, wed, thd, fri, sat, sun };

TEST_CASE("changes for enums")
{
}